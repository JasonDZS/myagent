# slide_agent_ws 环境变量示例
# 将本文件复制为 .env 并按需修改；
# 然后使用 `source .env`、direnv 或进程管理器读取。

# ==============================
# 数据库配置（二选一）
# ==============================

# 方案 A：SQLite（默认）
DB_TYPE=sqlite
# SQLite 数据库文件路径（示例）
SQLITE_DATABASE=./data/your.sqlite

# 方案 B：MySQL（如使用请取消注释，并注释掉上面的 SQLite 配置）
# DB_TYPE=mysql
# MYSQL_HOST=127.0.0.1
# MYSQL_PORT=3306
# MYSQL_USER=root
# MYSQL_PASSWORD=your_password
# MYSQL_DATABASE=your_database
# MYSQL_CHARSET=utf8mb4

# ==============================
# WebSocket 服务与流水线行为
# ==============================

# 并发求解页数（>0，默认 5）
SLIDE_WS_CONCURRENCY=5

# 在 plan.completed 中是否广播完整任务（默认 true）
SLIDE_WS_BROADCAST_TASKS=true

# 失败重试次数与间隔（默认 1 次，3.0 秒）
SLIDE_WS_MAX_RETRY=1
SLIDE_WS_RETRY_DELAY=3.0

# 是否启用规划确认与超时（秒，默认 true，600）
SLIDE_WS_REQUIRE_CONFIRM=true
SLIDE_WS_CONFIRM_TIMEOUT=600

# 说明：
# - 以上环境变量由 slide_agent_ws 内部读取；
# - 运行示例：
#     python -m slide_agent_ws.server --host 0.0.0.0 --port 8086
#   或（在该目录下直接运行脚本）：
#     python server.py --host 0.0.0.0 --port 8086
